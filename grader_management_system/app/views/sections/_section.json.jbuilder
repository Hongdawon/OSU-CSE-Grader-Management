json.extract! section, :id, :courseId, :sectionNumber, :instructionMode, :location, :startTime, :endTime, :startDate, :endDate, :meetingDays, :instructorName, :created_at, :updated_at
json.url section_url(section, format: :json)
